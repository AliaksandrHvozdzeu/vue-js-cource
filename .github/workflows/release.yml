name: Release

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches:
      - main
      - master

jobs:
  release:
    name: Publish Release
    if: >-
      ${{ github.event.workflow_run.conclusion == 'success' &&
          github.event.workflow_run.event != 'pull_request' &&
          github.event.workflow_run.head_repository.full_name == github.repository }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      actions: read

    steps:
      - name: Download CI artifact
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
        with:
          name: dist
          path: dist
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: Read version
        id: version
        run: |
          echo "version=$(jq -r .version dist/package.json)" >> "$GITHUB_OUTPUT"
          echo "name=$(jq -r .name dist/package.json)" >> "$GITHUB_OUTPUT"

      - name: Check if release already exists
        id: check
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="v${{ steps.version.outputs.version }}"
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release ${TAG} already exists, skipping."
            echo "should_release=false" >> "$GITHUB_OUTPUT"
          else
            echo "Release ${TAG} will be created."
            echo "should_release=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Archive production build
        if: steps.check.outputs.should_release == 'true'
        run: |
          rm -f dist/package.json
          cd dist
          zip -r "../${{ steps.version.outputs.name }}-${{ steps.version.outputs.version }}.zip" .

      - name: Create GitHub Release
        if: steps.check.outputs.should_release == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="v${{ steps.version.outputs.version }}"
          ARCHIVE="${{ steps.version.outputs.name }}-${{ steps.version.outputs.version }}.zip"

          gh release create "$TAG" "$ARCHIVE" \
            --target "${{ github.event.workflow_run.head_sha }}" \
            --title "$TAG" \
            --generate-notes
